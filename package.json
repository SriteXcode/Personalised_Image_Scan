{
  "name": "personalised_image_scan",
  "version": "1.0.0",
  "scripts": {
    "client": "npm run dev --prefix client",
    "server": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "commonjs",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "mongoose": "^8.16.4"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "concurrently": "^8.2.0"
  }
}
